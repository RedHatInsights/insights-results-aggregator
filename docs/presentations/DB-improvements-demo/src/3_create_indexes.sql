CREATE INDEX recommendation_0_idx
       ON recommendation_0 (org_id, cluster_id, rule_fqdn, error_key);

CREATE INDEX recommendation_1_idx
       ON recommendation_1 (org_id, cluster_id, rule_fqdn, error_key);

CREATE INDEX recommendation_2_idx
       ON recommendation_2 (org_id, cluster_id, rule_fqdn, error_key);

CREATE INDEX recommendation_3_idx
       ON recommendation_3 (org_id, cluster_id, rule_fqdn, error_key);

CREATE INDEX recommendation_4_idx
       ON recommendation_4 (org_id, cluster_id, rule_fqdn, error_key);
